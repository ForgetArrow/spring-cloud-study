server:
  port: 10121
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ForgetArrow/spring-cloud-study.git
          search-paths: cloud-config-repo
      label: master
  application:
    name: config-server